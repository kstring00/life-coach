const cards=[['Current Goal','Define the one outcome that matters most right now.'],['This Week’s Priority','One visible action to complete before the next session.'],['Next Session','Calendar integration placeholder.'],['Action Items','Track commitments from the last session.'],['Recent Reflection','Capture what worked, what did not, and what changed.'],['Resources','Worksheets, notes, and coaching tools.']];
export const metadata={title:'Client Portal'};
export default function PortalPage(){return <main><section className="section-shell page-hero"><p className="eyebrow">CLIENT PORTAL / CONCEPT</p><h1>Your progress, without the clutter.</h1><p>This is the front-end structure for a future authenticated client experience. No private client data is stored here yet.</p></section><section className="section-shell section-pad content-grid">{cards.map(([title,copy])=><article className="content-card glass" key={title}><h2>{title}</h2><p>{copy}</p></article>)}</section></main>}
